/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow strict-local
 */

import React, {useState} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  useColorScheme,
  Image,
  TouchableOpacity,
  Item,
  View,
  ImageBackground,
  FlatList,
  backgroundColor,
} from 'react-native';
import Icon from 'react-native-vector-icons/FontAwesome5';

export default function Tab2({navigation}) {
  return <View style={{flex: 1, margin: 24}}></View>;
}
